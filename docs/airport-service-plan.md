# #32 R-B 공항: 수요부터 실제 도착까지

상태: Draft 구현·통합 검증 진행 중이며 공항 전체 기능 완료가 아니다. 분기 기준039bc8e는 PR172를
포함하며, 부모의 검증/병합과 분리해 진행한다. localhost:3000과 라이브 저장은 건드리지 않는다.
공항 언락/에셋/통계만으로 운항 완료를 대신하지 않는다. 수도·국가·정치·외교·전쟁은
#32의 별도 잔여 요구이며 이 문서가 그 범위를 줄이지 않는다.

## 확인한 코드와 연결 위험

2026-09-04 logic101 통합: 유상 건설/운항에 이어 주민 접근·승하차·이탈·방문 중력·
고용주 배정과 실제 근무/귀가를 연결했다. 기내 재지시/진학 취소, 유한 좌석 대기,
전체 공항 폐쇄/회항 후 예약 정리와 실제 이벤트 저장을 회귀로 검증 중이다.
아래 단계별 과거 미연결 기록은 진행 이력이며 현재 완료 선언이 아니다. 서버/화면은
공항·노선·기체/게이트 인원과 관할 건설 조건을 스냅샷/배치로 연결했고, 별도 임시 DB의
실제 Chrome에서 재접속까지 확인했다. 아래 logic103 갱신으로 외관과 통제된 장기 운항을
검증했다. logic105에서는 아래처럼 지상 단절 첫 투자 신호를 실제 실패 의도로 연결하며,
자연 성장 빈도 주장과 통제 시나리오 검증은 구분한다.

### logic105: 지상 단절 첫 투자 근거

logic106은 후속 main efbeb2d의 양방향 말다툼 호감/거절 처리를 보존해 통합한다.
기존105 저장에는 argumentAffinity 기본값을 보충하고 main102의 사용자 지정값은
보존한다. 아래105 장기 감사 수치는 역사적 결과이며106의 궤적과 혼동하지 않는다.

- 자격·돈·빈 자원을 통과해 실제로 선택한 일반 이동이 땅길/현행 항공편 부재로 실패할 때만
  `air_trip_unserved`를 기록한다. 주거 출발 영역과 목적 시설 내부의 도달 가능성도 검사한다.
  지역 내 이동·무인 마을·공사/순찰/배송/개척단·고립된 실내 자원은 공항 투자 신호가 아니다.
- `transportStats.today.unservedAirTrips`에서 주민/방향 있는 마을쌍/하루를 중복 제거한다.
  역사와 공개 요약에는 ID가 아닌 건수만 남기고 기존14일 창을 따른다. 완료 방문·출발·탑승
  숫자에는 섞지 않는다. 무료 활동의 정당한 시도는 허용하지만 돈 없는 유료 식사는 기록하지 않는다.
- 완료 방문 또는 미충족 의도 중 하나가 독립적으로 `airportTripsMin`에 도달해야 한다.
  두 값을 더하지 않는다. 새 노선 부모 순위도 두 관측치 중 큰 값을 쓰므로 중복 합산하지 않는다.
  미래 승객을 예측하거나 수요 해제만으로 시설/기체/자금을 생성하지 않는다.
- `node bench/airport-long-audit.js 30 --unserved-bootstrap`은 과거 수요를 주입하지 않는다.
  초기 인구14명, 주택/식사 위치·욕구·재정·등급은 명시적으로 통제한다. 12명의 실제
  자율 이동 실패 후 유상 주문하고 기본 노동량을 완수한 뒤30일 운항/DB 재개/원장을 검사한다.
  직업 설정은 개통 시 근로 가능한 성인에게만 적용하고, 모든 근무/공사 시작의 자격을 확인한다.
  이것은 실제 규칙 연결의 통합 감사이며 자연적인 대도시/공항 성장 빈도 측정이 아니다.
  logic105 검증 결과: 완공 tick56737, 종료99937, 탑승/하차590회(방향293/297),
  실제 근무169회, SQL 이벤트83104건, 총자금636000 보존, 최종해시459bae14.
  인구는 고정하지 않는다. 매tick ID와 출생/이민/사망/이주 이벤트를 대조하며,
  최종15명은 초기14명 + 성장한 자녀1명 + 이민1명 - 사망1명으로 설명된다.
  인위적인 주민·욕구·자금 보충은 없으며 고용 이벤트와 근무 대상의 일치도 확인한다.

### logic103 통합 검증 갱신

후속 main1572cf2 통합(schema78/logic104)에서도 기본 유상 건설과30일 DB 재개 감사를
재실행했다: 완공 tick58451, 양방향83/80 탑승·하차163회, 실제 근무89회,
총자금540000 보존, SQL 이벤트13337건, 최종해시8f88ee33. main의 거절/갈등 행동을
보존한 결과이므로 아래 logic103의 궤적 숫자와 다르다. 접촉 행렬 없는 구버전 저장은
행렬 확장 전에 기존 대화 행렬을 독립 복사해 이관하며 schema1/3/13/36 회귀를 추가했다.

- 공항 제거 ID 영구 폐기·유상 재건설·기존 승객 물리적 회항을 검증했다.
- #176은 실행 가능한 긴급 식사/수면을 사교/여가 선호보다 우선한다. 장기 감사에서
  재현한 굶주림 고착을 해결하며, 고립 주민의 모든 성장 문제를 해결했다는 뜻은 아니다.
- 회전 footprint에 맞는 코드 기반 활주로·터미널·관제탑 외관을 구현했다. 실제 Chrome의
  snapshot/batch, 재접속/리로드, redraw 시 그래픽 중복 없음과 기체/게이트 표시를 확인했다.
- `bench/airport-long-audit.js`: 통제된 주민4명(마을당2명), 초기 시설·재정·과거 수요.
  양쪽 국고에서30000씩 차감하고 기본60000 노동씩 실제 자율 완공(tick58431)했다.
  이후30일 동안 욕구·돈·행동·수요 보충 없이 탑승/하차162회, 근무86회, 기체1을 관측했다.
  매tick 총자금540000 보존, 매일 SQL 스냅샷 재로드 후 같은 다음 날 실행, 이벤트13763건
  모든 필드의 순서 있는 SQL readback을 확인했다. 최종세계 해시4ec90f4c.
- 이전 주민2명(마을당1명) 조건은60일 내 양쪽 공항을 완공하지 못했다. #176 적용 후
  사망은 없어졌지만 한쪽 건설 노동0은 유지됐다. 위4명 감사는 이 고립 성장 사례나
  자연적인 공항 수요 발생 빈도를 입증하지 않는다.
- 공항 경로 조회는 지상보다 빠를 가능성이 없는 접근/이탈 시간 하한에서 BFS를 생략한다.
  장애물·대기·환승을 무시한 낙관적 하한이므로 기존 경로·선택·동률 규칙은 바뀌지 않는다.
  `bench/airport-query-cost.js`는 인구200의 합성 조회 부하이며 자연 성장/전체tick 비용이 아니다.

- `logic.tiers[3]`: 대도시 문턱120명. 관할 등급은 `governmentFor`가 권위이며 이웃의
  등급이나 국고를 빌려 해제/건설하면 안 된다.
- `zoneAllowedTypes` → 유상 zone → project 노동 → `addBuilding`이 실제 건설 경로다.
- `chooseVisit`/고용주 배정은 `sameRegion`/BFS를 선행한다. 보행 불가 목적지를 일찍
  버리면 바다를 건너는 공항이 있어도 아무도 이용하지 않는다.
- `startAction`과 `chooseRailJourney`는 보행 경로가 존재한다는 전제가 있다.
  비행 구간을 walking 경로로 넘기면 물/산 위에 보행 마모와 인사가 발생하는 오류가 된다.
- `transport-stats`의 municipalVisit은 실제 도착 시에만 세고, 미완료 출발/취소는 별개다.
- `rail`의 유한 좌석/실제 열차 좌표/하차 후 취소, #173의 이벤트 등록/1KB 저장 계약을
  항공에도 적용해야 한다. 보행 BFS의 N/E/S/W 좌표 계약은 바꾸지 않는다.

## 근거와 게임에 적용할 부분

1. [FAA, Forecast Review and Approval Instructions (2024)](https://www.faa.gov/airports/planning_capacity/Forecasts-Approval-Instructions):
   현재 활동과 가까운 시점의 근거를 개발 판단에 사용하고, 활동 수준을 투자 계기로
   삼도록 설명한다. 게임에서는 실제 관측한 이동량과 시설 수용량을 분리하고, 임의의
   미래 승객 수를 생성해 건설 근거로 쓰지 않는다. FAA의 실세계 승인 절차를 재현하지는 않는다.
2. [Li, Chow, Guo (2025), Runway capacity expansion planning for public airports under demand uncertainty](https://arxiv.org/abs/2502.02783):
   투자 시점/용량/수요 불확실성을 함께 다룬다. 확인 범위는 공개 초록과 서지정보다.
   여기서는 수요 신호와 공급 용량의 분리 원칙만 참고한다. GBM/Poisson 모델이나 논문의
   수치 결과를 게임의 결정적 수식으로 구현했다고 주장하지 않는다.
3. [Lee et al. (2026), Airport Terminal Passenger Queue Forecasting for Departure Gates and Security Checkpoints, v3](https://arxiv.org/abs/2606.07622v3):
   게이트/검색대의 대기 인원·대기 시간을 서로 다른 관측량으로 다룬다. 확인 범위는
   공개 초록과 서지정보다. 게임에서는 대기열 길이, 승차 수, 실제 기다린 tick을 별도로
   기록한다. Transformer 예측기를 이 게임에 넣거나 논문의 성능을 달성했다고 하지 않는다.

이하 알고리즘/수치는 게임 설계 선택이며 위 문헌이 그 수치를 입증하는 것은 아니다.

## 구현 계약 제안

### 수요와 유상 건설

- 관할 대도시이고 다른 마을이 존재해야 airport 건설 후보가 될 수 있다. 각 마을은
  자기 조건으로 판정한다. 상대 마을의 공항도 해당 관할 조건/비용/노동을 충족해야 한다.
- 최근 실제 마을 간 장거리 출발 수와 거리 총합을 관할별로 관측한다. 미완료 출발,
  실제 도착, 지상 우회 시간은 분리한다. 도달 불가 후보를 성공한 여행으로 세지 않는다.
- 기존14일 이동 관측창과 같은 유한 기간을 사용한다. 시간 경과만으로 누적 수요가
  영원히 커지는 대신, 기본값/문턱을 파라미터화하고 통제 fixture와 자연 성장 표본으로
  조정한다. 구체 문턱은 구현 전 계측/리뷰에서 확정한다.
- 해제는 무상 공항/비행기를 생성하지 않는다. zone 비용은 해당 국고에서 외부 유출로,
  공사 진행은 근로 가능한 실제 현장 주민의 노동으로 처리한다. footprint는 대학12×10보다
  큰 terminal/runway 조합을 사용하고 회전·도로·다리·기존 주택·예약 공터를 보존한다.
- 건설비에는 초기 운항 설비를 포함한다. 첫 운항 모델은 현재 공공 철도와 같은
  자본비용 기반 무임 공공 서비스다. 항공사·유료 티켓·연료·조종사 고용까지 구현한
  모델로 표시하지 않는다. 이를 도입할 경우 별도의 폐쇄 회계 계약과 공급 모델이 필요하다.

### 경로 선택과 승객 상태

- 지상 경로가 가능한 경우 기존 보행/자가용/철도 선택을 유지한다. 항공 후보는
  출발 공항 접근 BFS + 실제 시간표 대기 + 비행 + 도착 공항부터 목적지 BFS로 평가한다.
  단순 공항 간 거리만으로 항상 비행을 고르지 않는다. 동률에서는 기존 지상 선택을 보존한다.
- 보행 불가여도 양쪽 공항에 각각 접근/이탈이 가능하면 항공 후보를 검토한다.
  방문 목적지/고용주/실제 행동 시작의 도달 가능성 판정을 공통 여행 질의로 맞춘다.
  공항 없는 기존 세계는 경로/RNG/후보 해시를 유지해야 한다.
- 일반 승객 행동(근무/학습/소비/교류/여가)을 대상으로 한다. 개척단·화재 대응·공사·
  보호자 동행·실물 배송은 기존 특수 상태를 보존하며 이번 항공 모드에 몰래 태우지 않는다.
- 상태는 access → waiting_flight → flying → egress → 실제 시설 도착이다.
  탑승은 공항에 실제 도착한 순서, 동률 sim ID. 용량 초과자는 다음 운항을 기다린다.
- 비행 구간은 결정적인 항공 경로의 좌표를 순서대로 진행한다. 지형의 보행 가능 여부와
  무관하지만 경계 밖으로 나가지 않는다. 승객 좌표는 항공기를 따르며 지상 인사/마모/
  분실물 습득을 발생시키지 않는다. 좌표 점프를 시설 도착으로 처리하지 않는다.
- 일일 생애 전환/취업 변경은 기내 승객을 지상으로 순간이동시키지 않는다. 예약 해제 후
  목적지 공항 하차 시 행동을 취소한다. 모든 상태에서 중간 저장 재개가 동일해야 한다.

### 네트워크·운항 중단·자원 상한

- 공항 쌍마다 무조건 새 비행기를 만드는 완전 그래프는 피한다. 실제 수요가 있는 연결과
  유한 기체/좌석만 운영하고, 노선 수/기체 획득이 자본비용에 대응하도록 설계를 확정한다.
- 화재 등 터미널 폐쇄는 신규 탑승을 막는다. 이미 비행 중인 승객은 기체에 남아 안전한
  공항에 도착한 뒤 내린다. 도착 공항이 제거/불통이면 아래 확정 계약의 가까운 개방 공항
  우회, 전부 불가면 기내 대기를 사용한다. 바다 위 하차나 임의 지상 좌표 이동은 금지한다.
- 네트워크/시간표/기체/대기열은 저장 상태이고 이벤트 payload에는 경로 전체나 전체
  후보 공항 목록을 넣지 않는다. 새 이벤트는 발행과 동시에 등록/실제 Storage 검증을 한다.
- 항공 접근 BFS 비용을 계측하고 같은 질의에서 선택한 경로를 재사용한다. 장기 캐시나
  A*로 기존 경로 계약을 바꾸지 않는다. 시간표 주기가 지나치게 많은 질의를 만들면
  원인과 반례를 먼저 측정한다.

## 완료를 입증할 검증

1. 로컬 tier/다른 마을/실제 수요 경계, 유상 건설·초과 지출 방지, default 노동 완료,
   대학보다 큰 footprint4방향, 부지 소유/예약/도로와 기존 시설 보존.
2. 짧은 지상 이동은 지상 유지, 공항 접근/대기 때문에 비행이 느린 경우 제외,
   바다로 단절된 두 지역의 실제 방문/근무, 만석 대기 및 양방향 승하차.
3. access/waiting/flying/egress 각각의 저장 재개, 기내 진학·퇴직·사망·목적지 제거·
   화재 중단/복구·회항, 취소 후 예약/승객 ID 유령 제거, noPath/취소 계측.
4. 실제 유상 건설로 연 두 공항에서 개통 후30일 양방향 운항. 초기 건설/수요 통제는
   명시하고 자연 성장 빈도 주장과 분리한다. 매 tick 회계, 실제 도착, 모든 이벤트 저장을 검사한다.
5. 구버전 저장 migration 무드로우/무상 건설 없음, 공항 없는3시드 불변, 전체 회귀/CI,
   인구200 비용 비교, 실제 브라우저에서 공항·항공기·대기/탑승 표시/재접속.

## 독립 설계 리뷰 반영: 결정적인 네트워크 계약

검토자는 Codex이며 Claude 승인으로 간주하지 않는다. 구현이 없다는 지적은 현재 상태와
일치한다. 다음 계약으로 기체 수·환승·폐쇄 분기를 먼저 고정하고 통합 테스트로 검증한다.

- 네트워크는 건설 완료 순서로 확장하는 트리다. 첫 공항에는 운항 기체가 없으며 이후
  공항마다 이미 완공된 공항 하나에 연결하는 기체 한 대를 건설비에서 조달한다.
  신규 공항의 연결 대상은 최근 관측한 해당 두 마을 간 완료 이동 수 내림차순,
  직선 항공 거리 오름차순, 공항 ID 오름차순으로 선택한다. 기존 연결은 매 tick 다시
  뽑지 않는다. 완공 공항 N개의 신규 기체 총수는 max(0,N-1)이며 폐쇄/재개가 기체를
  새로 생성하지 않는다. 제거된 공항의 기체는 비활성 기록으로 남기고 임의 재배정하지 않는다.
- 각 연결에는 유한 좌석의 왕복 기체 하나가 있다. 경유 공항을 포함한 트리 경로를
  실제 여러 비행 구간으로 운항한다. 직통 두 마을만 지원하는 것으로 범위를 줄이지 않는다.
  시간표는 연결 개통 tick을 기준으로 고정하며 지연되면 지난 출발을 몰아서 실행하지 않는다.
- 탑승 순서는 실제 게이트 도착 tick, sim ID다. 환승객도 예외 없이 도착 뒤 최소
  연결 대기 tick을 충족하고 같은 대기열에 참여한다. 계획 ETA는 예약 보장이 아니다.
  만석이면 실제 다음 출발을 기다리며 기다린 시간을 별도 집계한다.
- 시간은 교통수단 선택에만 쓴다. 중력 가중치는 기존 지상 경로가 있으면 그 BFS 거리,
  지상 단절이면 접근 BFS + 각 비행 구간의 ceil(sqrt(dx²+dy²)) + 이탈 BFS라는
  공개된 물리 거리로 계산한다. 공항 없는 경우 기존 후보/RNG/경로가 변하지 않아야 한다.
- 목적지 폐쇄 시 유효한 개방 공항 중 현재 항공기 위치에서 직선 거리가 가장 짧은 곳으로
  우회하고 ID로 동률을 깬다. 개방 공항이 하나도 없으면 저장 가능한 기내 대기 상태를
  유지한다. 바다 위 하차/원래 출발지 순간이동은 금지한다. 공항이 다시 열리면 같은
  규칙으로 우회한다. 착륙 후 목적 시설까지 BFS가 없으면 기록된 no_path 취소로 끝낸다.
- 연결 한쪽이 닫히면 새 출발은 중단한다. 이미 기내인 승객은 위 규칙으로 안전한
  공항까지 간다. 승객 사망은 좌석/예약/대기열을 정리하지만 생존자를 순간이동시키지 않는다.
- 실제 최종 시설 도착만 기존 municipalVisit 성공에 포함한다. 공항 착륙·환승은 별도
  항공 통계다. 출발/실패한 시도를 완료 수요에 섞지 않는다.

리뷰의 "목적 마을 최소2곳" 제안은 채택하지 않는다. 두 마을 세계의 첫 항공 연결을
원천 차단하므로 다른 마을 한 곳이라는 원래 조건을 유지한다. 완성되지 않은 여행을
성공 수요로 위조하지 않는 원칙도 유지한다. 따라서 처음부터 지상으로 단절된 마을의
공항 투자 근거는 완료 이동량만으로는 발생하지 않는다. 이 부트스트랩 문제는 별도
관측 가능한 미충족 여행 의도와 성공 수요를 분리하는 설계가 필요하며 아직 미해결이다.

## 구현 전 계측으로 확정할 항목

- 수요 단위/창/문턱, 부지 크기와 회전 작업 위치, 자본비용/노동량.
- 수요가 없는 첫 두 공항 및 지상 단절 마을의 투자 근거. 인구만으로 실제 승객을
  생성하지 말고 미충족 의도 관측과 완료 이동량을 별도 필드/검증으로 유지할 것.
- 좌석 수/항공 속도/정차·환승 tick. 논문에서 도출한 상수라고 주장하지 않을 것.

이 미확정 항목을 건너뛰고 공항 완료를 선언하지 않는다.

## 구현 진행: 시간표와 환승 질의 기반

`sim/flight-schedule.js`는 정수 tick 왕복 시간표, 직선 항공 거리/위치, 다음 출발 질의다.
`sim/flight-itinerary.js`는 대기를 허용하는 FIFO 시간표 위에서 다구간 최단 도착시간을
찾는다. 출발 tick에 게이트 도착을 먼저 처리한 뒤 탑승을 마감하는 순서가 전제다.
환승 최소 시간과 폐쇄 공항을 고려하고 좌석 예약을 약속하지 않는다. 실제 만석은 이후
승객 상태기계에서 재대기해야 한다. 입력/출발 시각이 같으면 목록 순서와 저장 재개에
무관하게 같은 결과를 낸다. 순수 함수여서 기존 세계/RNG/BFS를 바꾸지 않는다.

집중12/12: 거리/역방향 위치, 출발·도착 경계, 여러 속도/정지시간의 시간표 일치,
실제 두 구간 환승 질의, 놓친 연결, 시간 우선 선택, ID 동률, 폐쇄/단절/순환/저장 동등성을
검증했다. **world tick/건설/기체 좌석/승객/저장 schema/UI에는 아직 연결하지 않았다.**
이 테스트는 전체 공항 기능 검증이나 실제 운항 증거가 아니다.

`sim/air-network.js`는 완공 등록당 최대 기체 한 대만 조달하는 트리 상태를 추가한다.
첫 공항은 기체0대, 공항10개는 연결/기체9대이며 재등록/폐쇄/재개는 새 기체를 만들지 않는다.
최근14일의 실제 완료 municipalVisits만 양방향 합산해 연결 대상을 선택한다. 오늘을
포함한14일이며 취소·출발·오래된/미래 날짜는 수요에 포함하지 않는다. 이 계층은 비용을
차감하거나 시설을 생성하지 않는다. **호출부가 유상 공사 완료 시에만 연결해야 하며 아직
그 호출부는 없다.** 집중18/18로 네트워크 상한·중복 등록·저장·동률·폐쇄 불변성을 검증했다.

정상 운항 승객 실행기 `sim/flight-passengers.js`는 모든 기체 이동/하차를 먼저 처리하고
그 뒤 FIFO 게이트 탑승을 처리한다. 유한 좌석 초과자는 다음 편을 기다리고, 환승 최소
시간은 예정 시각이 아니라 실제 하차 tick에서 계산한다. 기내 취소는 하차까지 좌표를
유지하며 게이트 취소는 탑승하지 않는다. 마지막 공항 하차는 `flight_arrived`일 뿐
목적 시설 행동 완료가 아니다. world 통합부가 이탈 보행과 예약 정리를 해야 한다.

승객/시간표/네트워크 및 기존 이벤트·화면 QA 집중53/53 통과. 실제 승하차·환승 이벤트를
1KB 경계와 DB commit/read로 검증했다. 세 이벤트 이름은 등록하고 전체 피드 문구를
연결했다(자주 발생하는 교통 이벤트로 기본 이야기 필터에는 넣지 않음).
정상 실행기에 blocked 링크를 넘기면 상태 변경 전에 거부한다. world tick에는 미연결이다.

후속 `flight-diversion.js`: 열린 원래 목적지는 유지하고 닫혔으면 현재 기체 좌표에서
가장 가까운 개방 공항으로 물리적으로 우회한다. 모두 닫혔으면 기내 대기 상태를 저장하며
공항 재개 시 우회한다. 우회 도중 대체 공항이 닫혀도 직전 실제 좌표부터 새 경로를 잡는다.
착륙은 `flight_diverted_arrival` 또는 취소일 뿐 기존 목적 시설에 도착했다는 뜻이 아니다.
world 통합은 아직 남아 있다.

독립 승객 리뷰 P2/P3 수정: 공통 순수 검증기가 중복 탑승 ID/사라진 주민을 정리하고,
실제 좌석 초과·중복 주민 ID·손상된 환승 경로·잘못된 탑승 시각/방향·누락 탑승 기록은
어떤 상태 변경/이벤트보다 먼저 거부한다. 정상/우회 양 실행기에 적용했다.
집중61/61 통과: 우회/전 공항 폐쇄/재개/기내 취소·저장 재개, 실제 우회 이벤트 DB 저장,
리뷰 반례의 무변경 거부를 포함한다. 세계 전체 운항 완료 검증으로 확대 해석하지 않는다.

`flight-reposition.js`는 하차 완료한 빈 기체만 원래 출발 공항까지 실제로 이동시킨다.
양쪽 공항이 열려 있고 복귀가 끝나면 `serviceEpochTick`에서 정차 시간을 새로 시작하며
역사적 `openedTick`은 보존한다. 새 필드가 없으면 기존 시간표 계산과 동일하다.
복귀 중 도착지가 다시 폐쇄되면 기내 대기/우회 경로로 돌아간다. 이미 내린 승객은
기체 복귀에 따라 이동하지 않는다. 집중67/67: 복귀 위치/시간표/중간 저장/재폐쇄/
승객 동반 거부 및 실제 재개 이벤트 DB 저장·피드 등록을 포함한다.
유상 건설/실제 world 단계 연결과 종합 서비스 검증은 여전히 완료되지 않았다.

`air-service.js`는 정상 운항/폐쇄/우회/빈 기체 복귀를 매 tick 한 실행 순서로 묶는다.
폐쇄된 노선의 게이트 주민은 현재 좌표에 남은 `flight_route_unavailable`로 넘기며,
world 호출부가 실제 경로 재탐색/예약 해제를 책임진다. 이미 하차한 주민은 기체 복귀에
따라 이동하지 않는다. 중복/건너뛴 tick은 상태 변경 전에 거부하고 기체 없는 세계에는
필드를 추가하지 않는다. 집중73/73 통과 후 최초 tick의 지상 빈 기체는 불필요한 우회를
시작하지 않게 보강하고 해당6/6을 재검증했다. 아직 실제 world tick 호출부는 없다.

건물 레시피에 airport20×12(회전 시12×20), gate2개를 추가했다. 대학12×10보다 큰
부지라는 게임 기본값이며 실제 활주로 치수나 터미널 처리량을 재현한 수치가 아니다.
도로/다리/철도/지형/기존 시설(잔디 위 공원 포함)을 덮지 않으며, 다른 유상 주문/공사와
큰 부지 예약 충돌을 양방향 검사한다. 공항 ID 카운터는 실제 생성 때만 증가하고 저장돼
철거 후 기존 노선 ID가 새 공항으로 잘못 이어지지 않는다. 집중12/12(기존 대학 포함).
airport는 아직 ZONEABLE/등급 허용 목록에 추가하지 않았으므로 이 레시피만으로
사용자 건설이 활성화되지는 않는다. 비용·수요 게이트·유상 공사 완료 연결은 다음 단계다.

후속 schema77/logic100: 이제 일반 유상 zone→실제 노동→완공→공항/기체 등록을 연결했다.
관할 tier3, 다른 주민 마을, 최근14일 장거리 마을 간 완료 도착12건을 요구한다.
기본 비용30000/노동60000/속도8/좌석8/정차10/환승10은 게임 설계 파라미터이며
논문에서 추정한 값이 아니다. 기본 노동량을 유지한 두 공항 실제 완공과 기체 이동,
회계 보존/저장 재개6/6 통과. 초기 마을·수요기록·자금은 통제 fixture이며 자연 발생
빈도 증거는 아니다. 실제 tick에서 항공 실행기를 호출하지만 주민의 여행 후보/접근·이탈/
생애 변화/예약·화면 연결은 여전히 남아 있다. Draft이며30일 운항 검증도 미완료다.
